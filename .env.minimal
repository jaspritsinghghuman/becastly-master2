# Minimal .env for Becastly - Just copy this and run!
# No external integrations required to start

POSTGRES_USER=becastly
POSTGRES_PASSWORD=becastly123
POSTGRES_DB=becastly
ENCRYPTION_KEY=change_this_to_32_random_chars
APP_URL=http://localhost

# That's it! The app will work with just these.
# Add integrations later through the web UI.
